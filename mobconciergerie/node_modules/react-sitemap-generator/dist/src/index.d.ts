import React = require('react');
export interface PathOption {
    slugs?: {
        [key: string]: string[];
    };
    ignore?: boolean;
    priority?: number;
    changefreq?: string;
}
interface GenerateSitemapProps {
    url: string;
    routes: React.FC[];
    output?: string;
    options?: {
        [key: string]: PathOption;
    };
}
export declare const generateSitemap: ({ url, routes, output, options, }: GenerateSitemapProps) => Promise<boolean>;
export {};
//# sourceMappingURL=index.d.ts.map